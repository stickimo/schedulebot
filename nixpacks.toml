[phases.setup]
nixPkgs = ["nodejs_20", "python311", "python311Packages.pip"]

[phases.install]
cmds = ["npm install --production", "pip install reportlab --break-system-packages"]

[start]
cmd = "node index.js"
